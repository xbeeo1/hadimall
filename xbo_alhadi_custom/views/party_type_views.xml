<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_party_type_list" model="ir.ui.view">
            <field name="name">party.type.list</field>
            <field name="model">party.type</field>
            <field name="arch" type="xml">
                <list>
                    <field name="name"/>
                </list>
            </field>
        </record>

        <!-- Form view -->
        <record id="view_party_type_form" model="ir.ui.view">
            <field name="name">party.type.form</field>
            <field name="model">party.type</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="name"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="action_party_type_name" model="ir.actions.act_window">
            <field name="name">Party Type</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">party.type</field>
            <field name="view_mode">list,form</field>
            <field name="context">{}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create Your First Party Type Name...!
                </p>
            </field>
        </record>

        <menuitem id="menu_action_party_type"
                  name="Party Type"
                  parent="contacts.menu_localisation"
                  action="action_party_type_name"
                  sequence="4"/>

    </data>
</odoo>
